<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.hwy"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>

    <uses-sdk tools:overrideLibrary="com.airbnb.viewmodeladapter"></uses-sdk>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".HwyApplication">

        <activity android:name=".ui.module.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.module.zhihu.ZhiHuDescribleActivity"/>

        <activity android:name=".ui.module.neteasy.NetEasyDetailActivity"></activity>

    </application>

</manifest>